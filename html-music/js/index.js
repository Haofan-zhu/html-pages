window.onload = function () {
  const myAudio = document.getElementById("music")
  const myPlay = document.getElementById("play")
  const myAccelerate = document.getElementById("accelerate")
  const myShowDown = document.getElementById("showDown")

// 可以用nodeJs 获取content/music里面的文件然后导入这个数组，这里先使用自制数组来使用
  let MusicArry = ['mom.mp3',];

// 查看是否获取到了元素
  console.log(myPlay);
  console.log(myAudio);

// // 为music 添加音乐
//   myAudio.autoplay = true
//   myAudio.src

// 
}